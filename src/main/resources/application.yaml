server:
  port: 8081

spring:
  application:
    name: gen-ai-java-spring
  ai:
    openai:
      api-key: ${OPENAI_API_KEY}
      chat:
        options:
          model: gpt-4o-mini
          max-tokens: 300
    hugginface:
      chat:
        api-key: ${Hugging_Face_Token}
        url:
        options:
            model: Qwen/Qwen2.5-Coder-7B-Instruct
            max-new-tokens: 1024
            temperature: 0.7
    Ollama:
      base-url: http://localhost:11434
      chat:
        options:
          model: llama3.2:1b
          max-new-token: 512
          temperature : 0.7

logging:
  level:
    root: INFO
    com.geni.java.spring: INFO
    org.springframework.ai: INFO